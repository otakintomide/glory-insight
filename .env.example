# Vite (embedded in client at build time — set in Cloudflare Pages → Settings → Environment variables for production builds)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: Moralis holder count for GCAT market snapshot (Pages: bind as secret; local: dev server + node server.js)
MORALIS_API_KEY=
